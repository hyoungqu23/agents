# Activity flow

Open `/activity` → scan recent records → expand the selected record's detail →
switch between Summary and Note → edit a local note or collapse details.

Sorting changes record order. Tab arrows change the detail panel; note arrow keys
edit the text. Data and notes remain local and reset on reload. This flow is the
existing behavior to retain while exploring the page layout.
