# Existing application scenario

The application already has an activity route, a normal production view, shared
header/sidebar components, and a project-supported development-only route boundary.
The exact framework and files must be discovered in the disposable test app supplied
by the evaluator. A response based on this description alone is a plan, not an executed
implementation.

The user asks for two in-app layout variants. Normal production builds must retain
their existing route behavior. Both variants include an editable note, a select menu,
and a tab group that uses arrow-key navigation. All edits are local demonstrations.
