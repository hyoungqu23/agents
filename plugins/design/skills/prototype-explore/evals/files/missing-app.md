# Activity request with no application checkout

The user describes an existing activity screen with a select menu and a keyboard tab
group, but has supplied no source files, development server or build configuration.
Only this request is available in the workspace.
